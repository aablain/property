
<li>
  {{rental.owner}}'s {{rental.type}} in {{rental.city}} with {{rental.bedrooms}} bedrooms and tile {{rental.tile}} and cats {{rental.cats}}
  {{#if isImageShowing}}
    <p><img src={{rental.image}} alt={{rental.type}} {{action 'imageHide'}}></p>
  {{else}}
    <button {{action 'imageShow'}}>Show Image</button>
  {{/if}}
  <button {{action 'delete' rental}}>Delete Rental</button>
</li>
